# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - WalletConnect Misidentifies React Native as Browser
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: Scope the property to the concrete failing case - React Native environment with document shim present
  - Test that `isReactNative()` returns `true` when `navigator.product === "ReactNative"` AND `typeof document !== "undefined"` (from Bug Condition in design)
  - Test that WalletConnect initializes without browser-mode errors in React Native environment
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found:
    - `isReactNative()` returns `false` when it should return `true`
    - WalletConnect throws errors about missing browser APIs
    - Verify `typeof document !== "undefined"` (confirms shim presence)
    - Verify `navigator.product === "ReactNative"` (confirms React Native environment)
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Shim Functionality Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for shared components using document/window shims
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements
  - Property-based testing generates many test cases for stronger guarantees
  - Test cases to observe and capture:
    - `document.addEventListener`, `document.removeEventListener` work without errors
    - `document.querySelector`, `document.querySelectorAll` work without errors
    - `window.addEventListener`, `window.removeEventListener`, `window.close` work without errors
    - `global.location`, `MutationObserver`, `CustomEvent` are accessible
    - Shared components between browser extension and mobile app function correctly
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [x] 3. Fix WalletConnect environment detection using patch-package

  - [x] 3.1 Create patch for @walletconnect/environment package
    - Install patch-package if not already installed (already in devDependencies)
    - Modify `node_modules/@walletconnect/environment/dist/cjs/env.js`
    - Reorder `isReactNative()` logic to check React Native indicators BEFORE checking if document is undefined
    - Add check for `navigator.product === "ReactNative"` as first condition
    - Add check for `global.HermesInternal` (React Native's JavaScript engine) as second condition
    - Keep original check as fallback for backwards compatibility
    - Generate patch file using `npx patch-package @walletconnect/environment`
    - Verify patch file is created in `patches/` directory
    - _Bug_Condition: isBugCondition(input) where input.platform === "ReactNative" AND input.hasGlobalDocument === true AND input.hasNavigatorWithReactNativeProduct === true AND isReactNative() === false_
    - _Expected_Behavior: isReactNative() returns true when navigator.product === "ReactNative" regardless of document existence_
    - _Preservation: All document/window shim functionality must remain unchanged (addEventListener, removeEventListener, querySelector, querySelectorAll, close, location, MutationObserver, CustomEvent)_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4_

  - [x] 3.2 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - WalletConnect Correctly Identifies React Native
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify `isReactNative()` returns `true` in React Native with document shim
    - Verify WalletConnect initializes without browser-mode errors
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.3 Verify preservation tests still pass
    - **Property 2: Preservation** - Shim Functionality Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all document/window shim methods still work correctly
    - Confirm shared components continue to function without errors
    - Confirm browser extension continues to work correctly

- [x] 4. Checkpoint - Ensure all tests pass
  - Run all tests to verify the fix is complete
  - Verify `isReactNative()` returns `true` in React Native environment with document shim
  - Verify WalletConnect operates in React Native mode (not browser mode)
  - Verify all document/window shim functionality is preserved
  - Verify shared components and browser extension continue to work correctly
  - If any issues arise, investigate and ask the user for guidance
