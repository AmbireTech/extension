// Keep imports extensionless:
//   import { BiometricsProvider, BiometricsContext } from '@common/contexts/biometricsContext'
// Webpack/Metro will prefer index.web.ts / index.native.ts and fall back to this file when needed.
export * from './biometricsContext'
