/**
 * The id of the app's only view. Mobile has a single window, so the view the controllers know
 * about is registered once by the WebView worker and lives for the whole session.
 */
export const MOBILE_VIEW_ID = 'default-mobile-app-view'
